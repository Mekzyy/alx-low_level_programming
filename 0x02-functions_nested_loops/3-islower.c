#include "main.h"

/*
 * _islower - prints 1 or 0 dependimg on input
 * @c: first parameter
 *
 * Description: prints all lowercase letters
 * Return: Always(0)
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z')
}
